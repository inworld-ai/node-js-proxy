/**
 * This module defines type constants used in event packets received by
 * the Inworld connection.
 *
 * @module
 */

const TYPE_CONTROL = 'control';
const TYPE_CUSTOM = 'custom';
const TYPE_DISCONNECTED = 'disconnected';
const TYPE_EMOTION = 'emotion';
const TYPE_PAUSED = 'paused';
const TYPE_TEXT = 'text';

export { TYPE_CONTROL, TYPE_CUSTOM, TYPE_DISCONNECTED, TYPE_EMOTION, TYPE_PAUSED, TYPE_TEXT }
